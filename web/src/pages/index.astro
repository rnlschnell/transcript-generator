---
import Layout from '../layouts/Layout.astro';
import DownloadForm from '../components/DownloadForm';
---

<Layout title="TikTok Video Downloader - Download TikTok Videos Without Watermark | KlipGrab" description="Free TikTok video downloader. Save TikTok videos to MP4 without watermark in HD quality. Download TikTok videos from link instantly on PC or mobile.">
  <!-- Hero Section -->
  <section class="hero">
    <div class="hero-bg">
      <div class="hero-gradient"></div>
      <div class="hero-grid"></div>
      <div class="hero-orb hero-orb-1"></div>
      <div class="hero-orb hero-orb-2"></div>
    </div>

    <div class="container">
      <div class="hero-content">
        <div class="hero-badge">
          <span class="badge-dot"></span>
          <span>Free TikTok video downloader</span>
        </div>

        <h1 class="hero-title">
          Download TikTok Videos
          <span class="title-highlight">Without Watermark</span>
        </h1>

        <p class="hero-subtitle">
          The fastest way to save TikTok videos to MP4. Download TikTok videos from any link
          in HD quality with audio — no watermark, completely free.
        </p>

        <div class="hero-form">
          <DownloadForm client:only="react" />
        </div>

        <div class="hero-stats">
          <div class="stat">
            <span class="stat-value">2M+</span>
            <span class="stat-label">Videos downloaded</span>
          </div>
          <div class="stat-divider"></div>
          <div class="stat">
            <span class="stat-value">50K+</span>
            <span class="stat-label">Happy users</span>
          </div>
          <div class="stat-divider"></div>
          <div class="stat">
            <span class="stat-value">HD</span>
            <span class="stat-label">Quality with sound</span>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- Features Section -->
  <section class="features" id="features">
    <div class="container">
      <div class="section-header">
        <span class="section-tag">Features</span>
        <h2>The Best Free TikTok Video Downloader</h2>
        <p>Download TikTok videos to MP4 with no watermark. Fast, free, and works on PC and mobile.</p>
      </div>

      <div class="features-grid">
        <div class="feature-card feature-card-highlight">
          <div class="feature-icon-wrap">
            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
              <circle cx="12" cy="12" r="10"/>
              <path d="m9 12 2 2 4-4"/>
            </svg>
          </div>
          <h3>Remove TikTok Watermark</h3>
          <p>Download TikTok videos without the watermark overlay. Get clean, professional-looking videos every time.</p>
        </div>

        <div class="feature-card">
          <div class="feature-icon-wrap">
            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
              <path d="M13 2L3 14h9l-1 8 10-12h-9l1-8z"/>
            </svg>
          </div>
          <h3>Fast TikTok to MP4</h3>
          <p>Convert and download TikTok to MP4 in seconds. No waiting, no buffering — just paste the URL and save.</p>
        </div>

        <div class="feature-card">
          <div class="feature-icon-wrap">
            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
              <path d="M9 18V5l12-2v13"/>
              <circle cx="6" cy="18" r="3"/>
              <circle cx="18" cy="16" r="3"/>
            </svg>
          </div>
          <h3>Download With Sound</h3>
          <p>Save TikTok videos with audio intact. Full quality video and sound, just like the original.</p>
        </div>

        <div class="feature-card">
          <div class="feature-icon-wrap">
            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
              <path d="M21 15v4a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2v-4"/>
              <polyline points="7,10 12,15 17,10"/>
              <line x1="12" x2="12" y1="15" y2="3"/>
            </svg>
          </div>
          <h3>HD High Quality</h3>
          <p>Download TikTok videos in HD quality. Get the highest resolution available for crystal-clear playback.</p>
        </div>
      </div>
    </div>
  </section>

  <!-- How It Works Section -->
  <section class="how-it-works" id="how-it-works">
    <div class="container">
      <div class="section-header">
        <span class="section-tag">How to save TikTok videos</span>
        <h2>Download TikTok Video From Link</h2>
        <p>Save any TikTok video without watermark in three simple steps.</p>
      </div>

      <div class="steps-container">
        <div class="steps-line"></div>

        <div class="step">
          <div class="step-number">
            <span>1</span>
          </div>
          <div class="step-content">
            <h3>Copy the TikTok URL</h3>
            <p>Open TikTok and tap "Share" then "Copy Link" on any video you want to download.</p>
          </div>
        </div>

        <div class="step">
          <div class="step-number">
            <span>2</span>
          </div>
          <div class="step-content">
            <h3>Paste & Download</h3>
            <p>Paste the TikTok video link above and click the download button to start.</p>
          </div>
        </div>

        <div class="step">
          <div class="step-number">
            <span>3</span>
          </div>
          <div class="step-content">
            <h3>Save to MP4</h3>
            <p>Your TikTok video downloads as MP4 directly to your device. No watermark!</p>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- Extension CTA Section -->
  <section class="extension-cta" id="extension">
    <div class="container">
      <div class="extension-card">
        <div class="extension-bg">
          <div class="extension-pattern"></div>
        </div>

        <div class="extension-content">
          <div class="extension-badge">
            <svg viewBox="0 0 24 24" fill="currentColor" width="16" height="16">
              <path d="M12 2C6.48 2 2 6.48 2 12s4.48 10 10 10 10-4.48 10-10S17.52 2 12 2zm-1 17.93c-3.95-.49-7-3.85-7-7.93 0-.62.08-1.21.21-1.79L9 15v1c0 1.1.9 2 2 2v1.93zm6.9-2.54c-.26-.81-1-1.39-1.9-1.39h-1v-3c0-.55-.45-1-1-1H8v-2h2c.55 0 1-.45 1-1V7h2c1.1 0 2-.9 2-2v-.41c2.93 1.19 5 4.06 5 7.41 0 2.08-.8 3.97-2.1 5.39z"/>
            </svg>
            <span>TikTok Download Extension</span>
          </div>

          <h2>One-Click TikTok Video Downloader</h2>
          <p>Download TikTok videos on PC even faster. Our Chrome extension adds a download button directly to TikTok — just click to save videos without watermark.</p>

          <div class="extension-buttons">
            <a href="/extension" class="btn btn-primary">
              <svg viewBox="0 0 20 20" fill="currentColor" width="18" height="18">
                <path d="M10.75 2.75a.75.75 0 00-1.5 0v8.614L6.295 8.235a.75.75 0 10-1.09 1.03l4.25 4.5a.75.75 0 001.09 0l4.25-4.5a.75.75 0 00-1.09-1.03l-2.955 3.129V2.75z"/>
                <path d="M3.5 12.75a.75.75 0 00-1.5 0v2.5A2.75 2.75 0 004.75 18h10.5A2.75 2.75 0 0018 15.25v-2.5a.75.75 0 00-1.5 0v2.5c0 .69-.56 1.25-1.25 1.25H4.75c-.69 0-1.25-.56-1.25-1.25v-2.5z"/>
              </svg>
              Get Free Extension
            </a>
            <span class="extension-note">Free to install</span>
          </div>
        </div>

        <div class="extension-visual">
          <div class="browser-mockup">
            <div class="browser-bar">
              <div class="browser-dots">
                <span></span>
                <span></span>
                <span></span>
              </div>
              <div class="browser-address">tiktok.com</div>
            </div>
            <div class="browser-content">
              <div class="mockup-video">
                <div class="mockup-overlay">
                  <div class="download-button-mockup">
                    <svg viewBox="0 0 20 20" fill="currentColor" width="14" height="14">
                      <path d="M10.75 2.75a.75.75 0 00-1.5 0v8.614L6.295 8.235a.75.75 0 10-1.09 1.03l4.25 4.5a.75.75 0 001.09 0l4.25-4.5a.75.75 0 00-1.09-1.03l-2.955 3.129V2.75z"/>
                      <path d="M3.5 12.75a.75.75 0 00-1.5 0v2.5A2.75 2.75 0 004.75 18h10.5A2.75 2.75 0 0018 15.25v-2.5a.75.75 0 00-1.5 0v2.5c0 .69-.56 1.25-1.25 1.25H4.75c-.69 0-1.25-.56-1.25-1.25v-2.5z"/>
                    </svg>
                    Download
                  </div>
                </div>
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- FAQ Section -->
  <section class="faq" id="faq">
    <div class="container">
      <div class="section-header">
        <span class="section-tag">FAQ</span>
        <h2>Frequently Asked Questions</h2>
        <p>Everything you need to know about our TikTok video downloader.</p>
      </div>

      <div class="faq-grid">
        <div class="faq-item">
          <h3>How do I download TikTok videos without watermark?</h3>
          <p>Simply copy the TikTok video URL, paste it into our downloader above, and click download. We automatically remove the watermark and save the video in high quality MP4 format.</p>
        </div>

        <div class="faq-item">
          <h3>Is this TikTok video downloader free?</h3>
          <p>Yes! KlipGrab is a free TikTok video downloader. You get 10 free downloads without signing up. Create a free account for additional downloads.</p>
        </div>

        <div class="faq-item">
          <h3>Can I download TikTok videos with sound?</h3>
          <p>Absolutely. Our TikTok downloader saves videos with audio intact. You get the full video with sound, exactly as it appears on TikTok.</p>
        </div>

        <div class="faq-item">
          <h3>How do I download TikTok videos on PC?</h3>
          <p>You can download TikTok videos on PC using our web-based downloader or Chrome extension. Just paste the video URL or use our extension for one-click downloads directly from TikTok.</p>
        </div>

        <div class="faq-item">
          <h3>What video quality can I download?</h3>
          <p>We download TikTok videos in HD quality — the highest resolution available. Videos are saved as MP4 files that play on any device.</p>
        </div>

        <div class="faq-item">
          <h3>Do I need to install anything?</h3>
          <p>No installation required for our web downloader. Just paste your TikTok URL and download. For faster downloads on PC, try our free Chrome extension.</p>
        </div>
      </div>
    </div>
  </section>

  <!-- Final CTA -->
  <section class="final-cta">
    <div class="container">
      <h2>Ready to Download TikTok Videos?</h2>
      <p>Start saving your favorite TikToks without watermark. Free, fast, and high quality.</p>
      <a href="/download" class="btn btn-primary btn-lg">
        Download TikTok Video Free
        <svg viewBox="0 0 20 20" fill="currentColor" width="18" height="18">
          <path fill-rule="evenodd" d="M3 10a.75.75 0 01.75-.75h10.638L10.23 5.29a.75.75 0 111.04-1.08l5.5 5.25a.75.75 0 010 1.08l-5.5 5.25a.75.75 0 11-1.04-1.08l4.158-3.96H3.75A.75.75 0 013 10z" clip-rule="evenodd"/>
        </svg>
      </a>
    </div>
  </section>
</Layout>

<style>
  /* Hero Section */
  .hero {
    position: relative;
    padding: 5rem 0 6rem;
    overflow: hidden;
  }

  .hero-bg {
    position: absolute;
    inset: 0;
    z-index: 0;
  }

  .hero-gradient {
    position: absolute;
    top: -50%;
    left: -25%;
    width: 150%;
    height: 200%;
    background: radial-gradient(ellipse at 30% 20%, rgba(6, 182, 212, 0.08) 0%, transparent 50%),
                radial-gradient(ellipse at 70% 80%, rgba(249, 115, 22, 0.05) 0%, transparent 40%);
  }

  .hero-grid {
    position: absolute;
    inset: 0;
    background-image:
      linear-gradient(rgba(15, 23, 42, 0.03) 1px, transparent 1px),
      linear-gradient(90deg, rgba(15, 23, 42, 0.03) 1px, transparent 1px);
    background-size: 60px 60px;
    mask-image: radial-gradient(ellipse at 50% 50%, black 20%, transparent 70%);
  }

  .hero-orb {
    position: absolute;
    border-radius: 50%;
    filter: blur(80px);
    opacity: 0.4;
  }

  .hero-orb-1 {
    top: 10%;
    right: 10%;
    width: 300px;
    height: 300px;
    background: var(--cyan-400);
  }

  .hero-orb-2 {
    bottom: 20%;
    left: 5%;
    width: 200px;
    height: 200px;
    background: var(--coral-400);
  }

  .hero-content {
    position: relative;
    z-index: 1;
    max-width: 720px;
    margin: 0 auto;
    text-align: center;
  }

  .hero-badge {
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
    background: var(--primary-muted);
    color: var(--cyan-600);
    padding: 0.5rem 1rem;
    border-radius: 100px;
    font-size: 0.875rem;
    font-weight: 600;
    margin-bottom: 1.5rem;
  }

  .badge-dot {
    width: 6px;
    height: 6px;
    background: var(--primary);
    border-radius: 50%;
    animation: pulse 2s ease-in-out infinite;
  }

  @keyframes pulse {
    0%, 100% { opacity: 1; transform: scale(1); }
    50% { opacity: 0.5; transform: scale(1.2); }
  }

  .hero-title {
    font-size: clamp(2.5rem, 6vw, 4rem);
    font-weight: 700;
    line-height: 1.1;
    margin-bottom: 1.25rem;
    letter-spacing: -0.03em;
  }

  .title-highlight {
    display: block;
    background: linear-gradient(135deg, var(--cyan-500) 0%, var(--cyan-400) 50%, var(--coral-500) 100%);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
  }

  .hero-subtitle {
    font-size: 1.125rem;
    color: var(--text-secondary);
    line-height: 1.7;
    max-width: 540px;
    margin: 0 auto 2.5rem;
  }

  .hero-form {
    max-width: 560px;
    margin: 0 auto 3rem;
  }

  .hero-stats {
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 2rem;
  }

  .stat {
    text-align: center;
  }

  .stat-value {
    display: block;
    font-family: var(--font-display);
    font-size: 1.75rem;
    font-weight: 700;
    color: var(--slate-900);
    letter-spacing: -0.02em;
  }

  .stat-label {
    font-size: 0.8125rem;
    color: var(--text-muted);
  }

  .stat-divider {
    width: 1px;
    height: 32px;
    background: var(--border);
  }

  /* Section Headers */
  .section-header {
    text-align: center;
    max-width: 560px;
    margin: 0 auto 4rem;
  }

  .section-tag {
    display: inline-block;
    font-size: 0.75rem;
    font-weight: 600;
    text-transform: uppercase;
    letter-spacing: 0.1em;
    color: var(--primary);
    margin-bottom: 0.75rem;
  }

  .section-header h2 {
    font-size: 2.25rem;
    margin-bottom: 0.75rem;
  }

  .section-header p {
    font-size: 1.0625rem;
    color: var(--text-secondary);
  }

  /* Features */
  .features {
    padding: 6rem 0;
    background: var(--background-secondary);
  }

  .features-grid {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: 1.5rem;
  }

  .feature-card {
    background: white;
    padding: 2rem;
    border-radius: var(--radius-lg);
    border: 1px solid var(--border-subtle);
    transition: all 0.3s ease;
  }

  .feature-card:hover {
    transform: translateY(-4px);
    box-shadow: var(--shadow-lg);
    border-color: transparent;
  }

  .feature-card-highlight {
    background: var(--slate-900);
    color: white;
    border-color: transparent;
  }

  .feature-card-highlight p {
    color: var(--slate-300);
  }

  .feature-icon-wrap {
    width: 48px;
    height: 48px;
    display: flex;
    align-items: center;
    justify-content: center;
    background: var(--primary-muted);
    border-radius: var(--radius-md);
    margin-bottom: 1.25rem;
  }

  .feature-icon-wrap svg {
    width: 24px;
    height: 24px;
    color: var(--primary);
  }

  .feature-card-highlight .feature-icon-wrap {
    background: rgba(6, 182, 212, 0.2);
  }

  .feature-card h3 {
    font-size: 1.125rem;
    margin-bottom: 0.5rem;
    font-family: var(--font-display);
  }

  .feature-card p {
    font-size: 0.9375rem;
    color: var(--text-secondary);
    line-height: 1.6;
  }

  /* How it Works */
  .how-it-works {
    padding: 6rem 0;
  }

  .steps-container {
    display: flex;
    justify-content: center;
    gap: 4rem;
    position: relative;
    max-width: 900px;
    margin: 0 auto;
  }

  .steps-line {
    position: absolute;
    top: 28px;
    left: 50%;
    transform: translateX(-50%);
    width: 60%;
    height: 2px;
    background: linear-gradient(90deg, var(--primary), var(--coral-500));
    opacity: 0.3;
  }

  .step {
    flex: 1;
    text-align: center;
    position: relative;
  }

  .step-number {
    width: 56px;
    height: 56px;
    margin: 0 auto 1.5rem;
    background: white;
    border: 2px solid var(--border);
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    position: relative;
    z-index: 1;
    transition: all 0.3s ease;
  }

  .step:hover .step-number {
    border-color: var(--primary);
    background: var(--primary);
    transform: scale(1.1);
  }

  .step-number span {
    font-family: var(--font-display);
    font-size: 1.25rem;
    font-weight: 700;
    color: var(--slate-900);
    transition: color 0.3s ease;
  }

  .step:hover .step-number span {
    color: white;
  }

  .step-content h3 {
    font-size: 1.125rem;
    margin-bottom: 0.5rem;
  }

  .step-content p {
    font-size: 0.9375rem;
    color: var(--text-secondary);
    max-width: 220px;
    margin: 0 auto;
  }

  /* Extension CTA */
  .extension-cta {
    padding: 6rem 0;
    background: var(--background-secondary);
  }

  .extension-card {
    position: relative;
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 4rem;
    align-items: center;
    background: var(--slate-900);
    border-radius: var(--radius-xl);
    padding: 4rem;
    overflow: hidden;
  }

  .extension-bg {
    position: absolute;
    inset: 0;
    overflow: hidden;
  }

  .extension-pattern {
    position: absolute;
    top: -50%;
    right: -20%;
    width: 600px;
    height: 600px;
    background: radial-gradient(circle, var(--cyan-500) 0%, transparent 60%);
    opacity: 0.1;
  }

  .extension-content {
    position: relative;
    z-index: 1;
  }

  .extension-badge {
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
    background: rgba(6, 182, 212, 0.15);
    color: var(--cyan-400);
    padding: 0.5rem 1rem;
    border-radius: 100px;
    font-size: 0.8125rem;
    font-weight: 600;
    margin-bottom: 1.5rem;
  }

  .extension-content h2 {
    color: white;
    font-size: 2rem;
    margin-bottom: 1rem;
  }

  .extension-content p {
    color: var(--slate-300);
    font-size: 1.0625rem;
    line-height: 1.7;
    margin-bottom: 2rem;
    max-width: 400px;
  }

  .extension-buttons {
    display: flex;
    align-items: center;
    gap: 1rem;
  }

  .extension-note {
    font-size: 0.8125rem;
    color: var(--slate-400);
  }

  .extension-visual {
    position: relative;
    z-index: 1;
  }

  .browser-mockup {
    background: white;
    border-radius: var(--radius-lg);
    overflow: hidden;
    box-shadow: var(--shadow-xl);
  }

  .browser-bar {
    display: flex;
    align-items: center;
    gap: 1rem;
    padding: 0.75rem 1rem;
    background: var(--slate-100);
    border-bottom: 1px solid var(--border);
  }

  .browser-dots {
    display: flex;
    gap: 6px;
  }

  .browser-dots span {
    width: 10px;
    height: 10px;
    border-radius: 50%;
    background: var(--slate-300);
  }

  .browser-dots span:first-child { background: #ff5f57; }
  .browser-dots span:nth-child(2) { background: #ffbd2e; }
  .browser-dots span:nth-child(3) { background: #28ca42; }

  .browser-address {
    flex: 1;
    background: white;
    padding: 0.375rem 0.75rem;
    border-radius: 6px;
    font-size: 0.8125rem;
    color: var(--slate-500);
  }

  .browser-content {
    padding: 1.5rem;
    background: var(--slate-50);
  }

  .mockup-video {
    aspect-ratio: 16/9;
    background: linear-gradient(135deg, var(--slate-200) 0%, var(--slate-300) 100%);
    border-radius: var(--radius-md);
    position: relative;
    display: flex;
    align-items: center;
    justify-content: center;
  }

  .mockup-overlay {
    position: absolute;
    inset: 0;
    display: flex;
    align-items: flex-end;
    justify-content: flex-end;
    padding: 1rem;
  }

  .download-button-mockup {
    display: flex;
    align-items: center;
    gap: 0.375rem;
    background: var(--accent);
    color: white;
    padding: 0.5rem 0.875rem;
    border-radius: 6px;
    font-size: 0.75rem;
    font-weight: 600;
    animation: bounceIn 0.5s ease 1s backwards;
  }

  @keyframes bounceIn {
    0% { transform: scale(0); opacity: 0; }
    60% { transform: scale(1.1); }
    100% { transform: scale(1); opacity: 1; }
  }

  /* FAQ Section */
  .faq {
    padding: 6rem 0;
  }

  .faq-grid {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: 2rem;
    max-width: 900px;
    margin: 0 auto;
  }

  .faq-item {
    background: var(--background-secondary);
    padding: 1.75rem;
    border-radius: var(--radius-lg);
    border: 1px solid var(--border-subtle);
  }

  .faq-item h3 {
    font-size: 1rem;
    font-family: var(--font-display);
    margin-bottom: 0.75rem;
    color: var(--text-primary);
  }

  .faq-item p {
    font-size: 0.9375rem;
    color: var(--text-secondary);
    line-height: 1.6;
  }

  /* Final CTA */
  .final-cta {
    padding: 6rem 0;
    text-align: center;
    background: var(--background-secondary);
  }

  .final-cta h2 {
    font-size: 2.25rem;
    margin-bottom: 0.75rem;
  }

  .final-cta p {
    font-size: 1.0625rem;
    color: var(--text-secondary);
    margin-bottom: 2rem;
  }

  .btn-lg {
    padding: 1rem 2rem;
    font-size: 1rem;
  }

  /* Responsive */
  @media (max-width: 1024px) {
    .features-grid {
      grid-template-columns: repeat(2, 1fr);
    }

    .extension-card {
      grid-template-columns: 1fr;
      text-align: center;
    }

    .extension-content p {
      max-width: 100%;
    }

    .extension-buttons {
      justify-content: center;
    }

    .extension-visual {
      display: none;
    }

    .faq-grid {
      grid-template-columns: 1fr;
    }
  }

  @media (max-width: 768px) {
    .hero {
      padding: 3rem 0 4rem;
    }

    .hero-stats {
      flex-direction: column;
      gap: 1.5rem;
    }

    .stat-divider {
      width: 40px;
      height: 1px;
    }

    .features-grid {
      grid-template-columns: 1fr;
    }

    .steps-container {
      flex-direction: column;
      gap: 2rem;
    }

    .steps-line {
      display: none;
    }

    .extension-card {
      padding: 2.5rem;
    }
  }
</style>
